<% provide(:title, @movie.title) %>
<div class="row">
	<div class="col-lg-4">
		<div class="thumbnail">
			<%= image_tag @movie.image_url.to_s %>
		</div>
	</div>
	<div class="caption movie-info">
		<h4><%= @movie.title %></a>
		</h4>
		<p><%= @movie.genre.name %> / <%= @movie.duration %> min</p>
		<p>Director: <%= @movie.director %></p>
		<p>Description: <%= @movie.description %></p>
	</div>
</div>

